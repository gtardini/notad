<%= form_for(@view) do |f| %>
  <% if @view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@view.errors.count, "error") %> prohibited this view from being saved:</h2>

      <ul>
      <% @view.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nad_id %><br />
    <%= f.text_field :nad_id %>
  </div>
  <div class="field">
    <%= f.label :viewedon %><br />
    <%= f.text_field :viewedon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
